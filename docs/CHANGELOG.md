# 版本记录

## v0.2.2-beta - 2026-03-26

围绕“浅色工作台 + 管理后台分区 + 混剪稳态能力”完成一轮前后端联动升级，重点改善任务操作路径、规划可读性和视觉模型容错。

### 主要变更

- 前台 `AppShell` / `TasksView` / `TaskCard` / `TaskRow` 全量改为浅色玻璃质感工作台，压缩导航层级并强化任务筛选与巡检路径。
- 新增 `HintBell` 轻提示组件，在新建页、任务页和详情页补充模式说明、输入质量提示与操作建议。
- 重构 `NewTaskView`，把任务创建流程拆成预设选择、素材与语义、混剪参数、编排预演和提交校验五段，降低多模式建单理解成本。
- 扩充混剪模式默认预设，新增音乐卡点、剧情冲突、城市漫游、治愈慢游四组模板，并按题材联动风格说明与提示词。
- 重构 `TaskDetailView` 为玻璃卡片式详情页，补充阶段进度、分镜方案切换、转场标签和任务日志解读。
- 前台主题切换下沉到 `App.vue`，按路由自动同步 admin/front document theme，并将 `/admin` 默认入口显式重定向到 `/admin/dashboard`。
- 重构后台框架页 `AdminShell`，改为浅色标准管理布局。
- 重构 `AdminDashboardView` 为总览卡片 + 异常任务表 + 可筛选日志表。
- 重构 `AdminTasksView` 为标准筛选工具栏 + 批量操作区 + 数据表格。
- 重构 `AdminTaskDetailView` 为基础信息、规划方案和日志诊断双栏结构。
- 重构 `ModelStatusStrip` 为只读配置表，强化模型/能力项可读性。
- 重构 `AdminSystemView`，支持任务 ID / 级别 / 阶段 / 关键词日志筛选。
- 后端新增 JSON 解析修复链路 `parse_json_object`，支持 fenced JSON、前缀噪声、缺失逗号和尾逗号修复，并在规划链路输出修复信息与错误上下文。
- 视觉分析阶段新增单素材解析失败重试与跳过机制；当部分素材连续返回非法 JSON 时，系统会记录 trace、跳过异常素材并继续整体规划。
- 抽取任务失败收口逻辑 `_fail_task`，补齐多素材组合源预处理阶段的异常落库与 trace 记录，避免任务在早期异常时丢失失败态。
- `media.py` 增强媒体工具错误提示并统一输出文件移动逻辑，避免跨文件系统替换失败。
- `apps/api/Dockerfile` 安装 `ffmpeg` 并切换 Debian 镜像源，保证容器内媒体分析和渲染依赖可用。

## v0.2.1-beta - 2026-03-26

围绕任务创建页完成“四角色三轮迭代”，重点修正参数一致性、模式联动和创建前校验。

### 主要变更

- 前端 `TaskPreset` 类型补齐 `editingMode`、`mixcutContentType`、`mixcutStylePreset`。
- 新建页预设卡片新增模式标识，并按当前模式筛选预设。
- 新增旅行分镜混剪回退预设，补齐混剪默认入口。
- 修复短剧模式多素材冲突：
  - 短剧模式下追加多素材会自动切换为混剪模式。
  - 切回短剧模式会自动保留主素材并移除额外素材。
- 新建页新增“提交校验”清单（标题、素材数量、时长区间、产出数量）。
- 提交按钮与校验状态联动，未通过时禁用提交并提示失败项。
- 文档同步：
  - `docs/UI_UX.md` 更新真实字段、模板枚举与流程说明。
  - `docs/API.md` 更新创建任务示例和模式约束。
  - 新增 `docs/TASK_CREATION_ROLE_ITERATIONS.md` 记录三轮迭代结果。

## v0.2.0-beta - 2026-03-25

基于多 agent 协同完成三轮连续产品迭代，补齐任务工作台、任务复用和模板化建单能力。

### V1：任务可见性增强

- 后端任务详情新增规划方案 `plan`、素材摘要 `source`、开始/结束时间、重试次数、已完成输出数。
- 前端任务详情页新增阶段卡片、规划方案区、素材元数据展示。
- 任务卡片和状态组件升级为更高信息密度的工作台样式。

### V2：任务管理与复用

- `GET /api/v1/tasks` 支持 `q`、`status`、`platform` 过滤。
- 新增 `POST /api/v1/tasks/{taskId}/clone`，返回可用于新建页预填的任务草稿。
- 任务列表页支持搜索、状态筛选、平台筛选、按生命周期分组展示。
- 列表页和详情页都支持“复制参数”进入新建流程。

### V3：模板化建单与工作台指标

- 新增 `GET /api/v1/presets`，提供 4 个静态任务预设。
- 新建页新增预设模板卡片、参数摘要、历史任务复制提示。
- 任务列表页新增总任务、进行中、已完成、失败四类概览指标。
- 筛选状态同步到 URL query，方便回放和分享当前视图。

### 后续五个版本方向

- V4：模型就绪层。补充模型配置校验、运行时状态展示、失败原因细分，不做真实 token 自检。
- V5：字幕驱动切片。突出 SRT/VTT 与时间戳台词输入，增强语义切点规划。
- V6：批量运营工作台。支持按素材和模板批量创建、筛选与复用。
- V7：人工审核与微调。把规划方案从只读展示升级为可编辑、可重渲染。
- V8：策略复盘台。按模板、平台、失败阶段和输出结果做轻量策略分析。

## v0.1.0-alpha - 2026-03-25

首个可开发版本，完成项目基础骨架初始化。

### 已完成

- 初始化单仓目录结构。
- 确认技术栈：
  - 前端：Vue 3 + TypeScript + Vite + Vue Router + TailwindCSS
  - 后端：FastAPI
  - 数据库：MySQL（本地）
  - 队列/缓存：Redis（本地）
  - 存储：本地文件存储
  - 模型 provider：Qwen（可配置）
- 新增根配置文件 `config/app.toml` 和示例配置 `config/app.example.toml`
- 新增本地 MySQL / Redis 的 `docker-compose.yml`
- 初始化前端页面骨架：
  - 任务列表
  - 新建任务
  - 任务详情
- 建立运行时配置文件 `apps/web/public/runtime-config.json`
- 建立项目级文档目录

### 计划中

- 完成 FastAPI 后端 API、上传、任务状态、FFmpeg 流水线
- 完成产品范围、架构、UI/UX、测试计划文档
- 完成使用文档和 API 文档
