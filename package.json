{
  "name": "jiandou",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "web:dev": "npm --prefix apps/web run dev",
    "web:build": "npm --prefix apps/web run build",
    "api:dev": ".venv/bin/python -m uvicorn app.main:app --app-dir apps/api --reload --reload-dir apps/api --reload-dir packages --reload-dir config --host 0.0.0.0 --port 8000",
    "worker:dev": ".venv/bin/python apps/worker/app/main.py"
  }
}
