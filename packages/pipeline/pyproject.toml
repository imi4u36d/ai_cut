[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-cut-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "ai-cut-shared==0.1.0",
  "ai-cut-db==0.1.0",
  "ai-cut-storage==0.1.0",
  "ai-cut-media==0.1.0",
  "ai-cut-ai==0.1.0",
  "SQLAlchemy>=2.0",
]
