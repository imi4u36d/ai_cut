[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-cut-storage"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "ai-cut-shared==0.1.0",
]
